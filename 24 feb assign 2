program on a factors of a number and its count:
#include<stdio.h>
#include<conio.h>
main()
{
int n,i,count=0,total count;
printf("enter n value:");
scanf("%d",&n);
printf("\n factors of %d are :",n);
for(i=1;i<=n;i++)
{
if(n%i==0)
{
printf("%d\t",i);
count=count+1;
}
}
printf("\n\n total factors count=%d",count);
return 0;
}
